# build-grant — grant (Fedora/COPR trigger branch)

Auto-managed **trigger branch** for the `grant` package: a push here fires
the grant COPR build via Packit, nothing else rebuilds. Canonical source is
`main` (do not edit here). Packaging: `fedora/grant/`.
